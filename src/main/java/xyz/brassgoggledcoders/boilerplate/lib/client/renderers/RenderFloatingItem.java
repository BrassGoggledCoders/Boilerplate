package xyz.brassgoggledcoders.boilerplate.lib.client.renderers;

/**
 * @author warlordjones
 *
 */

// TODO: Render Item?
public class RenderFloatingItem // extends RenderItem
{
	/*
	 * public static void render(double dx, double dy, double dz, float rotX,
	 * float rotY, float rotZ, ItemStack stack) { EntityItem entItem = new
	 * EntityItem(ClientHelper.world(), 0D, 0D, 0D, stack); // Without the below
	 * line, the item will spazz out RenderItem.renderInFrame = true;
	 * entItem.hoverStart = 0.0F; GL11.glTranslatef((float) dx, (float) dy,
	 * (float) dz); GL11.glRotatef(rotX, 1, 0, 0); GL11.glRotatef(rotY, 0, 1,
	 * 0); GL11.glRotatef(rotZ, 0, 0, 1); GL11.glScalef(1.3F, 1.3F, 1.3F);
	 * RenderManager.instance.renderEntityWithPosYaw(entItem, 0.0D, 0.0D, 0.0D,
	 * 0.0F, 0.0F); RenderItem.renderInFrame = false; }
	 * 
	 * public static void render(double dx, double dy, double dz, float rotX,
	 * float rotY, float rotZ, float scaleX, float scaleY, float scaleZ,
	 * ItemStack stack) { EntityItem entItem = new
	 * EntityItem(ClientHelper.world(), 0D, 0D, 0D, stack); // Without the below
	 * line, the item will spazz out entItem.hoverStart = 0.0F;
	 * RenderItem.renderInFrame = true; GL11.glTranslatef((float) dx, (float)
	 * dy, (float) dz); GL11.glRotatef(rotX, 1, 0, 0); GL11.glRotatef(rotY, 0,
	 * 1, 0); GL11.glRotatef(rotZ, 0, 0, 1); GL11.glScalef(scaleX, scaleY,
	 * scaleZ); RenderManager.instance.renderEntityWithPosYaw(entItem, 0.0D,
	 * 0.0D, 0.0D, 0.0F, 0.0F); RenderItem.renderInFrame = false; }
	 */
}